[workspace]
members = [
    "patches-core",
    "patches-modules",
    "patches-engine",
    "patches-integration-tests",
    "patches-player",
]
resolver = "2"

[profile.release]
debug = 1  # line-level debug info for profiling symbols
