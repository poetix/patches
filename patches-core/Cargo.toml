[package]
name = "patches-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "patches_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yml = "0.0.12"
